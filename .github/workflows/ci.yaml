name: Nix CI
"on":
  pull_request:
  push:
    branches: [master]
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch: {}

env:
  nix_conf: |
    access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
    substituters = https://cache.nixos.org/ https://nix-community.cachix.org https://cache.iog.io
    trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs= hydra.iohk.io:f/Ea+s+dFdN+3Y/G+FDgSq+a5NEWhJGzdjvKNGv0/EQ=
    keep-env-derivations = true
    keep-outputs = true

# TODO save from GC

jobs:
  # test:
  #   name: Test extensions
  #   permissions:
  #     actions: write
  #   strategy:
  #     matrix:
  #       os:
  #         - ubuntu-24.04
  #         - ubuntu-24.04-arm
  #         # too slow
  #         # - macos-13
  #         - macos-15
  #   runs-on: ${{ matrix.os }}
  #   continue-on-error: true
  #   steps:
  #     - name: Checkout this repo
  #       uses: actions/checkout@v4
  #     - name: Install Nix
  #       uses: nixbuild/nix-quick-install-action@v30
  #       with:
  #         nix_conf: ${{ env.nix_conf }}
  #     - uses: nix-community/cache-nix-action/restore@v6
  #       with:
  #         primary-key: nix-test-${{ runner.os }}-${{ runner.arch }}-${{ hashfiles('*.{nix,lock}', 'data/cache/vscode-marketplace-release.json') }}
  #         restore-prefixes-first-match: nix-test-${{ runner.os }}-${{ runner.arch }}-
  #     - name: Build VS Codium with extensions
  #       run: nix build
  #     - name: Check flake
  #       run: nix flake check
  #     - name: Save flake attributes from GC
  #       if: always()
  #       run: nix profile install .#saveFromGC.ci.jobs.test
  #     - uses: nix-community/cache-nix-action/save@v6
  #       if: always()
  #       with:
  #         primary-key: nix-test-${{ runner.os }}-${{ runner.arch }}-${{ hashfiles('*.{nix,lock}', 'data/cache/vscode-marketplace-release.json') }}
  #         gc-max-store-size-linux: 7500000000
  #         purge: true
  #         purge-prefixes: nix-test-${{ runner.os }}-${{ runner.arch }}-
  #         purge-created: 0
  #         purge-primary-key: never

  update:
    name: Update data and flakes
    permissions:
      actions: write
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    # if: github.ref_name == 'master'
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4
      - name: Install Nix
        uses: nixbuild/nix-quick-install-action@v30
        with:
          nix_conf: ${{ env.nix_conf }}
      - uses: nix-community/cache-nix-action/restore@main
        with:
          primary-key: nix-update-${{ hashfiles('*.nix', '*.lock') }}
          restore-prefixes-first-match: nix-update-
      # - name: Setup tmate session
      #   uses: mxschmitt/action-tmate@v3
      # - name: Configure git
  #       env:
  #         # required for gh
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       run: |
  #         git config --global user.email "github-actions[bot]@users.noreply.github.com"
  #         git config --global user.name "github-actions[bot]"
  #     - name: Pull latest changes
  #       run: git pull --rebase --autostash origin master
  #     - name: Update template flake locks
  #       run: |
  #         cd template
  #         nix flake update
  #     - name: Format Nix files
  #       run: nix fmt
      # - name: Save flake attributes from GC
      #   # updateExtensions* run gc
      #   if: always()
      - run: nix registry pin github:nixos/nixpkgs/3a05eebede89661660945da1f151959900903b6a
      # - run: nix profile install 
  #     - name: Update extensions
  #       run: nix run .#updateExtensions -- --config .github/config.yaml
      - run: nix profile install .#nvfetcher
      # - name: Update extra extensions
      #   run: nix run .#nvfetcher -- --help
      - run: nix store gc
      - run: nix run .#nvfetcher -- -c extra-extensions.toml  -o data/extra-extensions
  #     - name: Commit and push changes
  #       run: |-
  #         git pull --rebase --autostash origin master

  #         git add .

  #         git commit \
  #           -m "action" \
  #           -m "Update flake locks" \
  #           -m "Update extensions" \
  #           -m "Update extra extensions" \
  #           || echo "commit failed!"

  #         git push
      # - run: nix store gc
      - uses: nix-community/cache-nix-action/save@main
        if: always()
        with:
          primary-key: nix-update-${{ hashfiles('*.nix', '*.lock') }}
          gc-max-store-size-linux: 0
          purge: true
          purge-prefixes: nix-update-
          purge-created: 0
          purge-primary-key: never

  # test-template:
  #   name: Test template works
  #   needs:
  #     - update
  #     - test
  #   permissions:
  #     actions: write
  #   strategy:
  #     matrix:
  #       os:
  #         - ubuntu-24.04
  #         - ubuntu-24.04-arm
  #         # too slow
  #         # - macos-13
  #         - macos-15
  #   runs-on: ${{ matrix.os }}
  #   continue-on-error: true
  #   steps:
  #     - name: Checkout this repo
  #       uses: actions/checkout@v4
  #     - name: Install Nix
  #       uses: nixbuild/nix-quick-install-action@v30
  #       with:
  #         nix_conf: ${{env.nix_conf}}
  #     - name: Restore and cache Nix store
  #       uses: nix-community/cache-nix-action/restore@v6
  #       with:
  #         primary-key: nix-template-${{ runner.os }}-${{ runner.arch }}-${{ hashfiles('template/flake.nix', 'template/flake.lock') }}
  #     - name: Pull latest changes
  #       run: git pull --rebase --autostash origin master
  #     - name: Check template VSCodium
  #       run: nix develop template/#
  #     - name: Save flake attributes from GC
  #       if: always()
  #       run: nix profile install .#saveFromGC.ci.jobs.test-template
  #     - uses: nix-community/cache-nix-action/save@v6
  #       if: always()
  #       with:
  #         primary-key: nix-template-${{ runner.os }}-${{ runner.arch }}-${{ hashfiles('template/flake.nix', 'template/flake.lock') }}
  #         purge: true
  #         purge-prefixes: nix-template-${{ runner.os }}-${{ runner.arch }}-
  #         purge-created: 0
  #         purge-primary-key: never
